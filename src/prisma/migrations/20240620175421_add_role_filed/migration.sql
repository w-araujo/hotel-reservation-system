BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Guest] ADD [role] NVARCHAR(1000) NOT NULL CONSTRAINT [Guest_role_df] DEFAULT 'GUEST';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
